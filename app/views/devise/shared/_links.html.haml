- if controller_name != 'sessions'
  = link_to "すでにアカウントをお持ちの方はこちら", new_session_path(resource_name), class: 'btn'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "アカウントをお持ちでない方はこちら", new_registration_path(resource_name), class: 'btn'
  %br/
